# Copy this file to .streamlit/secrets.toml and fill in your real key.
# Never commit the real secrets.toml. This key grants access to send data to the Anthropic API on your behalf — treat it like a password.

ANTHROPIC_API_KEY = "sk-ant-your-key-here"
