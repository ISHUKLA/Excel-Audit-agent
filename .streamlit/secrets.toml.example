# Copy this file to .streamlit/secrets.toml and fill in your real key.
# .streamlit/secrets.toml must never be committed to version control --
# for local development, use a .env file instead (see .env.example).

ANTHROPIC_API_KEY = "sk-ant-your-key-here"
