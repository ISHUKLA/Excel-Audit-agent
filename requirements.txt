# Pinned versions verified locally and exercised in CI on Python 3.11 and 3.13
# (see .github/workflows/ci.yml). Python 3.11 is the Docker runtime.
# Do not loosen these versions without re-running the full test suite (pytest tests/).
#
# sqlite3 backs the audit log (core/audit_log.py). It is part of the Python
# standard library — no package line below, nothing to install.
#
# libreoffice is a SYSTEM dependency needed only to build test fixtures with
# real cached formula values. See README.md — it is not needed to run app.py
# or to run the test suite (fixtures are pre-baked and committed).

anthropic==0.125.0
Jinja2==3.1.6
networkx==3.2.1
numpy==2.0.2
openpyxl==3.1.5
pandas==2.3.3
pydantic==2.13.4
pytest==8.4.2
python-dotenv==1.2.1
PyYAML==6.0.2
RapidFuzz==3.13.0
streamlit==1.50.0
weasyprint==66.0
